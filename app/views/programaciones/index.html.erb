<%- model_class = Programacion -%>
<div class="page-header">
  <h1><center><p class="text-success">PROGRAMACIONES</p></center></h1>
<%= form_tag request.path, :method => 'get' do %>
  <%= content_tag :label do %>
    <%= image_tag('search2.png', :alt => '', :class => "img-circle" ) %>
    <%= hidden_field_tag :direction, params[:direction] %>
    <%= hidden_field_tag :sort, params[:sort] %>
    <%= text_field_tag :search, params[:search], :placeholder => "Buscar por Descripcion" %>
    <%= submit_tag "Consultar", :class => 'btn btn-inverse', :nombre => nil %>
    <% end %>
<% end %>
</div>
<table class="table table-hover table-bordered table-condensed pretty">
  <thead>
    <tr>
      <th><%= sortable "id" %></th>
      <th><%= sortable "Usuario" %></th>
      <th><%= sortable "Servicio" %></th>
      <th><%= sortable "Descripcion" %></th>
      <th>Acciones</th>
    </tr>
  </thead>
  <tbody>
     <%= render @programaciones %>
  </tbody>
  <tfoot>
    <tr>
      
    </tr>
  </tfoot>
</table>
<td><%= link_to 'Nueva Programacion', new_programacion_path, :class => 'btn btn-success', :remote => true %></td>
<br />
<br />
<div align="center">
<%= will_paginate @programaciones, :previous_label => '&laquo; Anterior', :next_label => 'Siguiente
&raquo;' %>
</div>
<p>
<em>Total Programaciones : </em><%= @programaciones.total_entries %>
</p>

